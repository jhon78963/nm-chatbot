# Vacío = peticiones same-origin → proxy Vite (ver vite.config.ts)
VITE_API_BASE_URL=

# Backend chatbot NM (PORT=8090 local o docker)
VITE_DEV_API_PROXY=http://localhost:8090

# Si Mongo/red bloquean backend local, descomenta la línea de abajo
# o créalo en .env.development.local (gitignored):
# VITE_DEV_API_PROXY=https://admision.uprit.edu.pe
